installCRDs: true

resources:
  requests:
    memory: "64Mi"
    cpu: "10m"

webhook:
  resources:
    requests:
      memory: "32Mi"
      cpu: "10m"

certController:
  resources:
    requests:
      memory: "32Mi"
      cpu: "10m"
