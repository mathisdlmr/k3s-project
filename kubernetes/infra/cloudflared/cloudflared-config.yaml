apiVersion: v1
kind: ConfigMap
metadata:
  name: cloudflared-config
  namespace: infra
data:
  config.yaml: |
    tunnel: mdlmr-tunnel
    protocol: http2
    credentials-file: /etc/cloudflared/creds/credentials.json
    ingress:
      - hostname: "*.mdlmr.fr"
        service: https://traefik.traefik.svc.cluster.local      
        originRequest:
          originServerName: mdlmr.fr
      - hostname: mdlmr.fr
        service: https://traefik.traefik.svc.cluster.local
        originRequest:
          originServerName: mdlmr.fr
      - service: http_status:404
