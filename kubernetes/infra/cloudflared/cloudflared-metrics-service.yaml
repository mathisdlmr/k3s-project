apiVersion: v1
kind: Service
metadata:
  name: cloudflared-metrics
  labels:
    app: cloudflared-tunnel
spec:
  selector:
    app: cloudflared-tunnel
  ports:
    - name: metrics
      port: 9090
      targetPort: metrics