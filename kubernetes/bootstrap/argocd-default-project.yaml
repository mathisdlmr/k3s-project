apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: default
  namespace: argocd
spec:
  sourceRepos:
    - "*"
  destinations:
    - server: "https://kubernetes.default.svc"
      namespace: argocd
  clusterResourceWhitelist:
    - group: "*"
      kind: "*"
